<div id="w">
    <section id="sform">
        <small>Note: No spaces or punctuation allowed. Searches are limited to one(1) keyword.</small>
        <input type="text" id="s" name="s" class="sfield" placeholder="Enter a search tag..." autocomplete="off">
    </section>
    <section id="photos">
        {% for res in results.photos.photo%}
{#            <img src="http://farm{{res.farm}}static.flickr.com/{{res.server}}/{{res.id}}_{{res.secret}}.jpg">#}
<a href="https://www.flickr.com/photos/{{res.owner}}/{{res.id}}" target="_blank"><img src="http://farm{{res.farm}}.static.flickr.com/{{res.server}}/{{res.id}}_{{res.secret}}_t.jpg"></a>
        {%endfor%}
    </section>
</div>